(() => {
'use strict';
fetch('game11.js')
  .then(r => { if (!r.ok) throw new Error(`Unable to load Build 11 base (${r.status})`); return r.text(); })
  .then(build11 => {
    const outerMarker = "    const blob = new Blob([build10], {type:'text/javascript'});";
    if (!build11.includes(outerMarker)) throw new Error('Build 12 patch missing: Build 11 injection point');
    const patchBlock = "    const build12Additions = \"  src = required(\\n    src,\\n    \\\" {id:'clean_break',title:'CLEAN BREAK',type:'lose_wanted',time:45,reward:2000,wanted:3}\\\\n];\\\",\\n    \\\" {id:'clean_break',title:'CLEAN BREAK',type:'lose_wanted',time:45,reward:2000,wanted:3},\\\\n {id:'crosstown',title:'CROSSTOWN',type:'checkpoint_run',spawn:{x:2280,y:1180},time:80,reward:3000,color:'#2ed66b',checkpoints:[[900,-1200],[0,650],[-1800,650]],radius:90,maxCheckpointSpeed:110,heat:1}\\\\n];\\\",\\n    \\\"CROSSTOWN mission definition\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"let campaignIndex=0,missionState='available',missionCar=null,missionCooldown=0,missionTimer=0,score=0,multiplier=1,levelComplete=false,sectorUnlocked=false,bestScore=0,justUnlocked=false,missionMenuOpen=false,missionMenuDismissed=false,minimapVisible=true;\\\",\\n    \\\"let campaignIndex=0,missionState='available',missionCar=null,missionCooldown=0,missionTimer=0,score=0,multiplier=1,levelComplete=false,sectorUnlocked=false,bestScore=0,justUnlocked=false,missionMenuOpen=false,missionMenuDismissed=false,minimapVisible=true,chainIndex=0,chainPoints=[];\\\",\\n    \\\"chained mission state\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"const activeMission=()=>['steal','deliver','destroy','escape'].includes(missionState);\\\",\\n    \\\"const activeMission=()=>['steal','deliver','destroy','escape','chain_steal','chain_drive'].includes(missionState);\\\",\\n    \\\"active chained mission states\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"if((missionState==='steal'||missionState==='destroy')&&missionCar)return{x:missionCar.x,y:missionCar.y,label:'TARGET CAR'};\\\",\\n    \\\"if((missionState==='steal'||missionState==='destroy'||missionState==='chain_steal')&&missionCar)return{x:missionCar.x,y:missionCar.y,label:missionState==='chain_steal'?'COURIER CAR':'TARGET CAR'};\\\",\\n    \\\"courier navigation target\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"if(missionState==='escape')return{x:RESPRAY.x+RESPRAY.w/2,y:RESPRAY.y+RESPRAY.h/2,label:'RES-PRAY'};\\\",\\n    \\\"if(missionState==='chain_drive'&&chainPoints[chainIndex]){const p=chainPoints[chainIndex];return{x:p[0],y:p[1],label:`CHECKPOINT ${Math.min(chainIndex+1,chainPoints.length)}/${chainPoints.length}`};}\\\\n if(missionState==='escape')return{x:RESPRAY.x+RESPRAY.w/2,y:RESPRAY.y+RESPRAY.h/2,label:'RES-PRAY'};\\\",\\n    \\\"checkpoint navigation target\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"campaignIndex=i;missionState='available';missionCooldown=0;missionTimer=0;missionCar=null;saveProgress();closeMissionMenu(false);startMission();\\\",\\n    \\\"campaignIndex=i;missionState='available';missionCooldown=0;missionTimer=0;missionCar=null;chainIndex=0;chainPoints=[];saveProgress();closeMissionMenu(false);startMission();\\\",\\n    \\\"mission selection resets chain\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"m.type==='steal_deliver'?'Steal + deliver':m.type==='destroy_target'?'Timed destruction':'Lose the police'\\\",\\n    \\\"m.type==='steal_deliver'?'Steal + deliver':m.type==='destroy_target'?'Timed destruction':m.type==='checkpoint_run'?'3-stage courier run':'Lose the police'\\\",\\n    \\\"mission terminal chain label\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"Keys 1\\u20133 select \\u00b7 Esc closes \\u00b7 complete jobs to unlock more\\\",\\n    \\\"Keys 1\\u20134 select \\u00b7 Esc closes \\u00b7 CROSSTOWN unlocks after level clear\\\",\\n    \\\"mission terminal four-key help\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"const n={'Digit1':0,'Digit2':1,'Digit3':2}[e.code];\\\",\\n    \\\"const n={'Digit1':0,'Digit2':1,'Digit3':2,'Digit4':3}[e.code];\\\",\\n    \\\"fourth mission hotkey\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"<div style=\\\\\\\"color:#efc94c;font-size:12px;font-weight:900;letter-spacing:.18em\\\\\\\">BUILD 11</div>\\\",\\n    \\\"<div style=\\\\\\\"color:#efc94c;font-size:12px;font-weight:900;letter-spacing:.18em\\\\\\\">BUILD 12</div>\\\",\\n    \\\"Build 12 front-end label\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"Navigation + mission terminal online\\\",\\n    \\\"Chained objectives + navigation online\\\",\\n    \\\"Build 12 front-end subtitle\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\" else if(m.type==='lose_wanted'){missionState='escape';wantedAtLeast(m.wanted||3);statusMessage=`${m.title} \\u2014 LOSE THE COPS`;}\\\\n statusTimer=2.4;\\\",\\n    \\\" else if(m.type==='lose_wanted'){missionState='escape';wantedAtLeast(m.wanted||3);statusMessage=`${m.title} \\u2014 LOSE THE COPS`;}\\\\n else if(m.type==='checkpoint_run'){missionState='chain_steal';chainIndex=0;chainPoints=m.checkpoints||[];spawnMissionCar(m);statusMessage=`${m.title} \\u2014 STEAL THE GREEN COURIER CAR`;}\\\\n statusTimer=2.4;\\\",\\n    \\\"start chained mission\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\" else if(missionState==='escape'&&wanted<=0)completeMission();\\\\n}\\\",\\n    \\\" else if(missionState==='escape'&&wanted<=0)completeMission();\\\\n else if(missionState==='chain_steal'){if(!missionCar||missionCar.destroyed)failMission('MISSION FAILED \\u2014 COURIER CAR DESTROYED');else if(inVehicle&&currentCar===missionCar){missionState='chain_drive';statusMessage=`COURIER CAR ACQUIRED \\u2014 CHECKPOINT 1/${chainPoints.length}`;statusTimer=2;}}\\\\n else if(missionState==='chain_drive'){if(!missionCar||missionCar.destroyed)failMission('MISSION FAILED \\u2014 COURIER CAR DESTROYED');else if(inVehicle&&currentCar===missionCar&&chainPoints[chainIndex]){const p=chainPoints[chainIndex],r=mission().radius||90,maxSpeed=mission().maxCheckpointSpeed||110;if(Math.hypot(missionCar.x-p[0],missionCar.y-p[1])<=r&&Math.abs(missionCar.speed)<=maxSpeed){if((mission().heat||0)>0&&chainIndex<chainPoints.length-1)raiseWanted(mission().heat);chainIndex++;if(chainIndex>=chainPoints.length)completeMission();else{statusMessage=`CHECKPOINT ${chainIndex}/${chainPoints.length} \\u2014 KEEP MOVING`;statusTimer=1.8;}}}}\\\\n}\\\",\\n    \\\"update chained mission\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\" if(missionState==='escape')return`CLEAR ALL WANTED HEADS${time}`;\\\",\\n    \\\" if(missionState==='escape')return`CLEAR ALL WANTED HEADS${time}`;\\\\n if(missionState==='chain_steal')return`STEAL THE GREEN COURIER CAR${time}`;\\\\n if(missionState==='chain_drive')return`CHECKPOINT ${Math.min(chainIndex+1,chainPoints.length)}/${chainPoints.length} \\u2014 SLOW BELOW ${mission().maxCheckpointSpeed||110}${time}`;\\\",\\n    \\\"chained mission HUD text\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"if(['steal','deliver','destroy'].includes(missionState)&&missionCar)\\\",\\n    \\\"if(['steal','deliver','destroy','chain_steal'].includes(missionState)&&missionCar)\\\",\\n    \\\"courier world marker\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\" if(missionState==='escape'){const p=playerTarget();ctx.strokeStyle='rgba(255,65,30,.75)';\\\",\\n    \\\" if(missionState==='chain_drive'&&chainPoints[chainIndex]){const cp=chainPoints[chainIndex];ctx.strokeStyle='#4ff2e8';ctx.lineWidth=5;ctx.beginPath();ctx.arc(cp[0],cp[1],58*pulse,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='rgba(255,220,70,.8)';ctx.lineWidth=3;ctx.beginPath();ctx.arc(cp[0],cp[1],39*pulse,0,Math.PI*2);ctx.stroke();}\\\\n if(missionState==='escape'){const p=playerTarget();ctx.strokeStyle='rgba(255,65,30,.75)';\\\",\\n    \\\"draw chained checkpoint\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    `function completeMission(){\\n const m=mission(),reward=m.reward*multiplier;score+=reward;multiplier=Math.min(multiplier+1,5);missionCar=null;missionTimer=0;campaignIndex++;\\n if(campaignIndex>=CAMPAIGN.length){\\n  const firstClear=!levelComplete&&score>=LEVEL_TARGET;\\n  if(score>=LEVEL_TARGET){levelComplete=true;sectorUnlocked=true;}\\n  campaignIndex=0;missionState='campaign_complete';missionCooldown=5;\\n  statusMessage=\\\`CAMPAIGN COMPLETE +\\\${reward}\\\${levelComplete?' \\u2014 LEVEL CLEARED':''}\\\`;statusTimer=4;\\n  justUnlocked=firstClear;saveProgress();if(firstClear)showUnlock();\\n }else{\\n  missionState='cooldown';missionCooldown=3;statusMessage=\\\`MISSION COMPLETE +\\\${reward} \\u2014 NEXT: \\\${mission().title}\\\`;statusTimer=3;saveProgress();\\n }\\n}`,\\n    `function completeMission(){\\n const m=mission(),completedId=m.id,reward=m.reward*multiplier;score+=reward;multiplier=Math.min(multiplier+1,5);missionCar=null;missionTimer=0;chainIndex=0;chainPoints=[];\\n if(completedId==='clean_break'){\\n  const firstClear=!levelComplete&&score>=LEVEL_TARGET;\\n  if(score>=LEVEL_TARGET){levelComplete=true;sectorUnlocked=true;}\\n  campaignIndex=0;missionState='campaign_complete';missionCooldown=5;\\n  statusMessage=\\\`CORE CAMPAIGN COMPLETE +\\\${reward}\\\${levelComplete?' \\u2014 LEVEL CLEARED':' \\u2014 REACH 5000'}\\\`;statusTimer=4;\\n  justUnlocked=firstClear;saveProgress();if(firstClear)showUnlock();\\n }else if(completedId==='crosstown'){\\n  campaignIndex=0;missionState='cooldown';missionCooldown=3;statusMessage=\\\`CROSSTOWN COMPLETE +\\\${reward}\\\`;statusTimer=3;saveProgress();\\n }else{\\n  campaignIndex=Math.min(campaignIndex+1,CAMPAIGN.length-2);missionState='cooldown';missionCooldown=3;statusMessage=\\\`MISSION COMPLETE +\\\${reward} \\u2014 NEXT: \\\${mission().title}\\\`;statusTimer=3;saveProgress();\\n }\\n}`,\\n    \\\"core clear unlocks post-clear mission\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"function failMission(msg){missionState='cooldown';missionCooldown=3;missionTimer=0;missionCar=null;statusMessage=msg;statusTimer=2.2;}\\\",\\n    \\\"function failMission(msg){missionState='cooldown';missionCooldown=3;missionTimer=0;missionCar=null;chainIndex=0;chainPoints=[];statusMessage=msg;statusTimer=2.2;}\\\",\\n    \\\"reset chain on failure\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"campaignIndex=0;missionState='available';missionCar=null;missionCooldown=0;missionTimer=0;score=0;multiplier=1;levelComplete=false;sectorUnlocked=false;bestScore=0;justUnlocked=false;loadProgress();updateHud();\\\",\\n    \\\"campaignIndex=0;missionState='available';missionCar=null;missionCooldown=0;missionTimer=0;score=0;multiplier=1;levelComplete=false;sectorUnlocked=false;bestScore=0;justUnlocked=false;chainIndex=0;chainPoints=[];loadProgress();updateHud();\\\",\\n    \\\"reset chained state\\\"\\n  );\\n\\n  src = required(\\n    src,\\n    \\\"DOWNTOWN ACCESS UNLOCKED</div>\\\",\\n    \\\"DOWNTOWN + CROSSTOWN UNLOCKED</div>\\\",\\n    \\\"unlock screen chained mission\\\"\\n  );\\n\";\n    build10 = build10.replace(marker, build12Additions + \"\\n\" + marker);\n";
    build11 = build11.replace(outerMarker, patchBlock + "\n" + outerMarker);
    const blob = new Blob([build11], {type:'text/javascript'});
    const script = document.createElement('script');
    script.src = URL.createObjectURL(blob);
    script.onload = () => URL.revokeObjectURL(script.src);
    document.head.appendChild(script);
  })
  .catch(err => {
    console.error(err);
    const detail = document.getElementById('detail');
    if (detail) detail.textContent = `BUILD 12 LOAD ERROR\n${err.message}`;
  });
})();
