if (!window.__gtaBuild30SlicedAssets) {
window.__gtaBuild30SlicedAssets = true;

// Build 30 bitmap art is stored as real repository assets, not an embedded data URI.
// Paths are relative to web/index.html so the same files work on RawGitHack/GitHub Pages
// and on a local static server.
window.__bitmap30AtlasPaths = Object.freeze({
  map: 'assets/build30/atlas.json',
  image: 'assets/build30/bitmap_atlas.png'
});
}
